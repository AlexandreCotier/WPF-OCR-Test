<Window x:Class="EasyHunt.HMI.Main.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EasyHunt.HMI.Main"
        mc:Ignorable="d"
        Title="MainView" Height="450" Width="800" Opacity="30">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="{Binding MyProp}"/>
        
        <Button HorizontalAlignment="Right" VerticalAlignment="Bottom" Grid.Row="1" Height="25" Width="100" Content="Click to screen" Command="{Binding ScreenCommand}"/>
    </Grid>
</Window>
